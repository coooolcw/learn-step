1.font-display
  https://developer.mozilla.org/zh-CN/docs/Web/CSS/@font-face/font-display
  在@font-face中定义,可统一各游览器,但有兼容性问题
  
2.font-spider
  https://github.com/aui/font-spider
  优化字体文件的大小,但是动态内容据悉无法优化
  而且似乎无法与webpack(包括vue)一起使用
  也很久没有更新了

3.fontfaceobserver
  https://github.com/bramstein/fontfaceobserver
  类似于1的方案,可以解决兼容性问题
  但是需要耦合js


还是需要找一个更好的解决方案,暂时留坑,现在初次加载问题比较严重,虽然会自动按照使用的(bold normal之类)自动加载
但是延时在本地都有接近1ms,上线后再进行观察
